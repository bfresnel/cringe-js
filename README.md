# cringe-js

## Requirements
* Visual Studio Code
* node LTS version
* npm

## How to run the project
* clone the project
* go in the project folder
* run ```npm install``` or ```npm ci```
* then run ```npm run start```
